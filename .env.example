# Copy to .env.local (git-ignored) for local development.
# The public URL of the deployed Cloudflare Worker that handles form
# submissions (see worker/). Not secret — it's just an endpoint URL.
VITE_CONTACT_ENDPOINT=https://querbeet-contact.<your-subdomain>.workers.dev
